# Copy to .env and fill in ONE key for your llm.provider in config.yaml (default: openai).
# Never commit .env — it is gitignored.

OPENAI_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=example_key

# Optional: Alpha Vantage (falls back to yfinance without key)
ALPHA_VANTAGE_API_KEY=
